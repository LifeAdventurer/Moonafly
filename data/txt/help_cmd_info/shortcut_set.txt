Usage: set "{replace string}" to "{shortcut string}"
    replace your text within the curly braces with your desired phrases
    note: do not include the curly braces
    this command sets the replacement behavior where whenever a message matches
    the SHORTCUT STRING, it will be replaced with teh REPLACE STRING
    note: both REPLACE STRING and SHORTCUT STRING cannot contain the
    character `"`
